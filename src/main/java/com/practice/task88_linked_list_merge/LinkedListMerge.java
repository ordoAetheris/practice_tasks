package com.practice.task88_linked_list_merge;

/**
 * ОДНОСВЯЗНЫЙ СПИСОК С НУЛЯ + СЛИЯНИЕ ДВУХ ОТСОРТИРОВАННЫХ БЕЗ КОПИРОВАНИЯ
 *
 * <p>Контекст: реальная задача технической секции Яндекс (СВЕРЕНО: Хабр 708318 —
 * «написать структуру односвязного списка; на входе два таких списка, на выходе один,
 * отсортированный, БЕЗ всякого копирования»). Проверяют работу с указателями руками
 * (без {@code LinkedList} из JDK) и переиспользование узлов.</p>
 *
 * <p>Задача:</p>
 * <ul>
 *   <li>Реализовать собственный односвязный список целых: {@code addLast(int)}, {@code toArray()},
 *       {@code head()} (доступ к первому узлу).</li>
 *   <li>Реализовать {@code merge(a, b)} — слияние ДВУХ уже отсортированных списков в один
 *       отсортированный, <b>переиспользуя существующие узлы</b> (не создавать новые узлы под данные,
 *       только перецеплять {@code next}). Исходные списки после merge можно считать «разобранными».</li>
 * </ul>
 *
 * <p>Уровень: Средний. Сложность merge: O(n+m) по времени, O(1) доп. памяти (кроме результата,
 * который собран из тех же узлов).</p>
 *
 * <p><b>Ловушки:</b> потеря хвоста при перецеплении; работа с пустыми списками; дубликаты значений;
 * один список полностью раньше другого; НЕ создавать новые Node под данные (в этом суть «без копирования»).</p>
 */
public class LinkedListMerge {

    /** Узел односвязного списка. */
    public static final class Node {
        public int val;
        public Node next;
        public Node(int val) { this.val = val; }
    }

    // TODO: поля списка (head, возможно tail и size)

    /** Добавить значение в конец. */
    public void addLast(int value) {
        // TODO: implement
    }

    /** Вернуть содержимое как массив (для проверок). */
    public int[] toArray() {
        // TODO: implement
        return new int[0];
    }

    /** Голова списка (или null). */
    public Node head() {
        // TODO: implement
        return null;
    }

    /**
     * Слить два ОТСОРТИРОВАННЫХ по возрастанию списка узлов в один отсортированный,
     * переиспользуя узлы (перецепляя next). Вернуть голову результата.
     *
     * @param a голова первого отсортированного списка (может быть null)
     * @param b голова второго отсортированного списка (может быть null)
     * @return голова слитого отсортированного списка
     */
    public static Node merge(Node a, Node b) {
        // TODO: implement (dummy head + перецепление, БЕЗ new Node под данные)
        return null;
    }

    public static void main(String[] args) {
        System.out.println("=== LinkedListMerge: Smoke Test ===");
        // TODO: собрать два отсортированных списка, слить, проверить порядок и что узлы переиспользованы.
    }
}

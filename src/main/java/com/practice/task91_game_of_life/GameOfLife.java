package com.practice.task91_game_of_life;

/**
 * ИГРА «ЖИЗНЬ» (клеточный автомат Конвея) — симуляция на поле M×N
 *
 * <p>Контекст: реальное тестовое задание техсекции Яндекса (СВЕРЕНО: GitHub GH-TIMe/Yandex.Interview —
 * симуляция клеточного автомата M×N, случайное/файловое начальное состояние, апдейт поля).
 * Проверяют работу с 2D-массивами, аккуратность на границах и чистую пошаговую логику.</p>
 *
 * <p>Правила (классические): для каждой клетки считаем живых соседей (8 соседей):</p>
 * <ul>
 *   <li>живая с 2 или 3 живыми соседями — выживает; иначе умирает;</li>
 *   <li>мёртвая ровно с 3 живыми соседями — оживает.</li>
 * </ul>
 *
 * <p>Реализовать:</p>
 * <ul>
 *   <li>Конструктор из {@code boolean[][]} (или {@code int[][]} 0/1) начального состояния.</li>
 *   <li>{@code step()} — посчитать СЛЕДУЮЩЕЕ поколение (важно: считать по СТАРОМУ состоянию,
 *       обновлять в НОВЫЙ буфер, не портить текущее во время подсчёта).</li>
 *   <li>{@code aliveCount()}, {@code isAlive(r,c)}, {@code render()} (текстовое поле).</li>
 *   <li>(опц.) загрузка из файла; N шагов подряд; тор (края замыкаются) как вариант.</li>
 * </ul>
 *
 * <p>Уровень: Средний. Ловушки (твой системный слабый угол — границы): соседи на КРАЯХ и УГЛАХ
 * (не выходить за массив), не обновлять поле «на лету» (иначе клетки видят уже изменённых соседей),
 * поле 1×1 / пустое, стабильные фигуры (блок не меняется) и осцилляторы (мигалка период 2).</p>
 */
public class GameOfLife {

    // TODO: поля — текущее состояние (boolean[][] / int[][]), rows, cols

    /**
     * @param initial начальное состояние (true/1 — живая клетка)
     */
    public GameOfLife(boolean[][] initial) {
        // TODO: implement (defensive copy начального поля)
    }

    /** Посчитать следующее поколение (по старому состоянию → в новый буфер). */
    public void step() {
        // TODO: implement (аккуратно с границами/углами)
    }

    /** Жива ли клетка. */
    public boolean isAlive(int row, int col) {
        // TODO: implement
        return false;
    }

    /** Число живых клеток. */
    public int aliveCount() {
        // TODO: implement
        return 0;
    }

    /** Текстовое представление поля (например '#'/'.'). */
    public String render() {
        // TODO: implement
        return "";
    }

    public static void main(String[] args) {
        System.out.println("=== GameOfLife: Smoke Test ===");
        // TODO: мигалка (blinker) период 2; блок 2x2 стабилен; проверить границы.
    }
}

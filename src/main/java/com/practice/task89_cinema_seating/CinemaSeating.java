package com.practice.task89_cinema_seating;

/**
 * РАССАДКА В КИНОТЕАТРЕ: СЕСТЬ МАКСИМАЛЬНО ДАЛЕКО ОТ ОСТАЛЬНЫХ
 *
 * <p>Контекст: реальная задача технической секции (СВЕРЕНО: Хабр 995600, VK/Яндекс-тип —
 * «массив из 0 и 1, зритель садится максимально далеко от остальных»). Проверяют аккуратность
 * с граничными случаями (пустой ряд, края) и чистоту логики.</p>
 *
 * <p>Задача (два уровня):</p>
 * <ul>
 *   <li><b>Статическая:</b> {@code maxDistanceSeat(int[] seats)} — ряд из 0 (свободно) и 1 (занято).
 *       Вернуть индекс свободного места, максимизирующий расстояние до ближайшего занятого.
 *       Учесть КРАЯ (место у края — расстояние считается до единственного соседа).
 *       Если занятых нет — сесть с краю (индекс 0). Если свободных нет — вернуть -1.</li>
 *   <li><b>Динамическая (усложнение):</b> класс с {@code seat()} — сажает зрителя на лучшее место и
 *       возвращает его индекс; {@code leave(int p)} — освобождает место p. Многократные seat/leave.</li>
 * </ul>
 *
 * <p>Уровень: Средний. Статическая версия — O(n). Динамическая — обсудить структуру
 * (для эффективного seat/leave напрашивается что-то лучше, чем линейный проход каждый раз).</p>
 *
 * <p><b>Ловушки (за них и валят — твой системный слабый угол):</b> пустой ряд, ряд из одного места,
 * все места свободны, все заняты, занятые только с одного края, места на самих краях,
 * два одинаково хороших места (детерминированный выбор — например меньший индекс).</p>
 */
public class CinemaSeating {

    /**
     * Индекс свободного места, максимально удалённого от ближайшего занятого.
     *
     * @param seats массив 0/1 (0 — свободно, 1 — занято)
     * @return индекс лучшего места; -1 если свободных нет
     */
    public static int maxDistanceSeat(int[] seats) {
        // TODO: implement (учесть края и случаи «нет занятых» / «нет свободных»)
        return -1;
    }

    // TODO (усложнение): динамическая рассадка
    //   public CinemaSeating(int n) { ... }
    //   public int seat()          // посадить на лучшее место, вернуть индекс (или -1 если полно)
    //   public void leave(int p)   // освободить место p

    public static void main(String[] args) {
        System.out.println("=== CinemaSeating: Smoke Test ===");
        // TODO: проверить [1,0,0,0,1,0,1] → индекс 2 (расстояние 2); края; пустой; полный.
    }
}
